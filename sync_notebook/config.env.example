# ─── Configuración del sync de la notebook ───────────────────────────────────
# Copiar este archivo como "config.env" en esta misma carpeta y completar con
# los datos reales. "config.env" NO se sube al repositorio (contiene
# contraseñas) — ver .gitignore.
#
# Este archivo se completa UNA VEZ en la notebook de la propietaria. La PC
# fija del local no necesita nada de esto.

# ─── PC servidor (la fija, del local) ────────────────────────────────────────
# 'auto' = se busca sola: por nombre de red (mDNS/NetBIOS), por la última
# encontrada, y si no, barriendo la subred. Confirma siempre con
# GET /api/v1/salud/ que del otro lado hay un Oga Porã con rol=servidor.
# Poner una IP o un nombre acá solo si se quiere forzar uno.
# Ver docs/descubrimiento_red.md
SERVIDOR_HOST=auto

# Nombres de red por los que se intenta encontrar al servidor, en orden.
SERVIDOR_NOMBRES=ogapora.local,ogapora
SERVIDOR_API_PUERTO=8000

# ─── Red del local (para saber si la notebook está adentro) ──────────────────
# Si la notebook está en otra red, el sync ni lo intenta.
RED_WIFI_SSID=OGA PORA
# MAC de la antena del router. Se lee sin entrar al panel del router:
#     powershell -c ". .\resolver_servidor.ps1; (Get-RedWifiActual).BSSID"
# Es más fuerte que el SSID (que cualquiera puede copiar). Vacío = no se chequea.
RED_WIFI_BSSID=

SERVIDOR_DB_PUERTO=5432
SERVIDOR_DB_NOMBRE=ceramica_db

# Usuario de SOLO LECTURA para el sync (no el usuario normal de la app).
# Se crea una única vez en la PC servidor — instrucciones en
# docs/sync_notebook.md. Nunca usar acá el usuario/contraseña de la app.
SERVIDOR_DB_USUARIO=notebook_sync
SERVIDOR_DB_PASSWORD=

# ─── Fotos de productos ──────────────────────────────────────────────────────
# La base guarda solo la RUTA de cada imagen, así que sin las fotos la
# notebook sincroniza el catálogo completo pero lo muestra con todas las
# imágenes rotas.
#
# Se bajan por HTTP del mismo servidor que ya sirve /media/. No hace falta
# configurar nada: si se deja vacío, el sync usa
# http://<SERVIDOR_HOST>:8000/media/. Solo completar si el backend del
# servidor se movió a otro puerto o dirección.
SERVIDOR_MEDIA_URL=

# Carpeta compartida de Windows con las fotos (\\SERVIDOR\media) — forma
# vieja, ya no hace falta. Si se completa y es accesible, el sync la usa en
# vez del HTTP (es más rápida la primera vez). Si no responde, cae solo al
# HTTP de arriba. Dejar vacío es lo normal.
SERVIDOR_MEDIA_UNC=

# ─── Base de datos LOCAL de la notebook (el espejo) ──────────────────────────
# Se sobreescribe por completo en cada sincronización exitosa.
LOCAL_DB_NOMBRE=ceramica_db
LOCAL_DB_USUARIO=ceramica_user
LOCAL_DB_PASSWORD=
LOCAL_DB_PUERTO=5432

# ─── Binarios de PostgreSQL ───────────────────────────────────────────────────
# Carpeta con pg_dump.exe y psql.exe. Dejar vacío si ya están en el PATH.
PG_BIN_DIR=C:\Program Files\PostgreSQL\15\bin
