# Herramientas de desarrollo. NO hacen falta para que el sistema funcione
# en el negocio: se instalan solo si se va a programar o revisar el codigo.
#
#   venv/Scripts/activate
#   pip install -r requirements-dev.txt
#
# Las pruebas automaticas (probar.bat) NO necesitan nada de aca: usan el
# corredor de pruebas que ya trae Django.

ruff==0.16.4

# Validacion del XML contra el XSD del SIFEN antes de transmitir
# (apps/facturacion/esquema.py). Python puro: no necesita compilador ni DLLs.
# Es opcional y esta aca y no en requirements.txt a proposito: hace falta para
# la campania de pruebas de habilitacion, no para vender. Ademas necesita el
# .xsd, que publica la DNIT aparte y no esta versionado.
xmlschema==3.4.3
