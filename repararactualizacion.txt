================================================================================
  REPARAR LA ACTUALIZACION - INSTRUCCIONES PARA LA PC SERVIDOR (OGAPORA)
================================================================================

  Escrito el: 12/09/2026, desde la notebook de la propietaria (DESKTOP-UAIGET9)
  Para:       la consola de Claude que corra EN LA PC SERVIDOR (OGAPORA)
  Objetivo:   destrabar el sync del catalogo. Hay 918 cambios de productos
              esperando desde el 27/08 que no pueden viajar.

  Este archivo es el canal entre las dos consolas. Al final hay una seccion
  "RESPUESTA DEL SERVIDOR" para anotar lo que se encontro; despues se commitea
  y la notebook lo lee.


--------------------------------------------------------------------------------
 0. SITUACION - leer antes de tocar nada
--------------------------------------------------------------------------------

  Dos equipos: la PC del local (OGAPORA, servidor, manda en stock/ventas/caja)
  y la notebook de la propietaria (espejo, solo edita el catalogo y lo empuja).
  El diseno completo esta en docs/sync_bidireccional.md.

  Lo verificado EN VIVO desde la notebook el 12/09/2026:

    OK     ogapora.local responde y resuelve (IPv6 fe80::8a22:c43:3903:cbc9)
    OK     GET /api/v1/salud/ devuelve rol=servidor, nombre=OGAPORA
           (esto antes daba 404 - o sea que el servidor YA tiene codigo nuevo)
    OK     /api/v1/sync/estado/, /catalogo/ y /cambios/ existen (dan 403, no 404)
    FALLA  los tres dan 403 "Token de sincronizacion ausente o invalido",
           incluso mandando el X-Sync-Token de la notebook

  Diagnostico: el SYNC_TOKEN del servidor NO es el mismo que el de la notebook.

  Sabemos que el servidor tiene *alguno* configurado y que no esta vacio,
  porque apps/sync/permissions.py usa un mensaje distinto cuando falta:
  "La sincronizacion esta deshabilitada: falta SYNC_TOKEN en el .env". Nos
  devuelve el otro mensaje. Asi que o son dos tokens distintos, o alla se
  edito el .env y nunca se reinicio daphne (decouple lee el .env al arrancar).

  ESE ES EL UNICO BLOQUEO. Todo lo demas, del lado de la notebook, esta sano:
  564 productos / 666 variantes / 666 filas de stock, 0 variantes huerfanas,
  las 89 fotos de referencia ya vinculadas, 212 tests en verde.


--------------------------------------------------------------------------------
 1. RESPALDO - no saltear
--------------------------------------------------------------------------------

  Se puede correr con el sistema andando:

      cd C:\ceramica_final
      respaldo\respaldo.bat D:\        :: si hay pendrive
      respaldo\respaldo.bat            :: sin pendrive, va a ..\respaldos\

  Anotar la carpeta que crea (respaldo_AAAAMMDD_HHMMSS) en la seccion de
  respuesta del final. Si algo sale mal: respaldo\restaurar.bat


--------------------------------------------------------------------------------
 2. TRAER EL CODIGO
--------------------------------------------------------------------------------

      cd C:\ceramica_final
      git status
      git pull

  La notebook esta en 8420d61 ("Revierte el salto automatico a listo en
  Pedidos, reconstruye dist"), que ya incluye:
    - iniciar_servicios.ps1 / detener.bat (daphne y Vite arrancan ocultos)
    - el ticket impreso con direccion, telefono y correo
    - el contacto comercial corregido en settings.py

  Si git status muestra cambios locales sin commitear en el servidor, PARAR y
  anotarlos abajo: puede haber trabajo hecho ahi que nadie subio. No hacer
  checkout ni stash sin avisar.


--------------------------------------------------------------------------------
 3. EL TOKEN - esto es lo que arregla el problema
--------------------------------------------------------------------------------

  El token NO va escrito en este archivo a proposito: el archivo viaja por
  GitHub y el token es un secreto compartido. Hay que pasarlo a mano.

  a) En la NOTEBOOK, la propietaria lo saca con:

         type C:\Users\usuario\ceramica_final\backend\.env | findstr SYNC_TOKEN

     Son 43 caracteres (secrets.token_urlsafe(32)). El de la notebook es el
     que manda: es el que ya esta en el .env de alla y el que va a usar la
     tarea programada.

  b) En el SERVIDOR, ver que hay hoy:

         type C:\ceramica_final\backend\.env | findstr "SYNC_TOKEN NODO_ROL"

     Tiene que quedar:

         NODO_ROL=servidor
         SYNC_TOKEN=<los mismos 43 caracteres que la notebook>

     Sin comillas, sin espacios alrededor del "=", sin espacio al final de la
     linea. La comparacion es hmac.compare_digest: un solo caracter distinto y
     sigue dando 403.

  c) Para comparar los dos sin mostrarlos enteros, correr esto en CADA equipo
     y comparar las dos salidas (largo + los primeros 4 caracteres):

         powershell -Command "$t=(Get-Content C:\ceramica_final\backend\.env | Select-String '^SYNC_TOKEN=').ToString().Split('=')[1].Trim(); Write-Host $t.Length 'caracteres, empieza' $t.Substring(0,4)"

     En la notebook la ruta es C:\Users\usuario\ceramica_final\backend\.env
     (alla ya se verifico que son 43 caracteres).


--------------------------------------------------------------------------------
 4. MIGRACIONES Y REINICIO
--------------------------------------------------------------------------------

      cd C:\ceramica_final\backend
      venv\Scripts\activate
      python manage.py migrate
      python manage.py migrate --database=sync

  La segunda es la del registro de cambios del sync, que vive en un SQLite
  aparte (sync.sqlite3) porque el pg_dump borra ceramica_db entera.

  Despues, reiniciar SI O SI - el .env se lee al arrancar:

      cd C:\ceramica_final
      detener.bat
      iniciar.bat

  Desde 8420d61 los dos arrancan ocultos, sin ventana. Los registros quedan en
  logs\daphne.log y logs\vite.log, y el PID en logs\*.pid.


--------------------------------------------------------------------------------
 5. VERIFICAR (en el servidor, contra si mismo)
--------------------------------------------------------------------------------

      curl -s http://localhost:8000/api/v1/salud/

  Tiene que decir "rol": "servidor". Y con el token:

      powershell -Command "$t=(Get-Content C:\ceramica_final\backend\.env | Select-String '^SYNC_TOKEN=').ToString().Split('=')[1].Trim(); (Invoke-WebRequest -Uri 'http://localhost:8000/api/v1/sync/estado/' -Headers @{'X-Sync-Token'=$t} -UseBasicParsing).StatusCode"

  Tiene que dar 200. Si da 403 contra si mismo, el .env no se releyo: repetir
  detener.bat + iniciar.bat.

  Avisar a la notebook cuando de 200. Desde alla se confirma y se empuja con:

      python manage.py sync_comparar --servidor ogapora.local
      python manage.py sync_empujar  --servidor ogapora.local --simular
      python manage.py sync_empujar  --servidor ogapora.local

  El empuje lo corre la NOTEBOOK, no el servidor. Va de a lotes de 500, y si se
  corta el WiFi lo ya confirmado queda marcado y el proximo intento sigue desde
  ahi.


================================================================================
 6. !! ATENCION - NO CORRER cargar_lote_facturas EN EL SERVIDOR !!
================================================================================

  En docs/carga_final/README.md y en las notas viejas quedo escrito que las
  cantidades del lote de agosto habia que darlas de alta en el servidor con:

      python manage.py cargar_lote_facturas --margen 40     (sin --sin-stock)

  ESA INSTRUCCION QUEDO MAL y no hay que ejecutarla. El problema es de orden, y
  se verifico leyendo el codigo el 12/09/2026:

  - Si se corre ANTES del empuje: crea en el servidor 110 productos con sus
    propios uid. Despues llega el empuje con los 110 de la notebook y
    apps/sync/conciliacion.py NO los fusiona, porque Producto no tiene clave
    natural en CLAVES_NATURALES (solo la tienen Marca, Acabado, Categoria y
    Variante). Producto esta en CAMPOS_REGENERABLES, o sea que al chocar el
    codigo se le genera uno nuevo. Resultado: el catalogo queda DUPLICADO.

  - Si se corre DESPUES del empuje: no duplica, pero tampoco carga el stock.
    En cargar_lote_facturas.py:304-310, si ya existe una Variante con ese
    (producto__nombre, color) escribe "= ya existe" y hace continue: se saltea
    la fila entera, el movimiento de stock incluido.

  Ninguno de los dos ordenes da el resultado buscado. Las cantidades del lote
  de agosto (180 variantes en 110 productos) necesitan otra via: un comando que
  recorra el mismo CSV, busque cada variante por (nombre_producto, color) y le
  registre el stock con Stock.registrar_movimiento(). Ese comando NO EXISTE
  todavia.

  QUE HACER: hacer el empuje del catalogo (pasos 1 a 5) y PARAR AHI. El stock
  es una decision aparte, que la propietaria tiene que ver con la consola de la
  notebook. No improvisar una carga de stock desde el servidor.


--------------------------------------------------------------------------------
 7. SI ALGO SALE MAL
--------------------------------------------------------------------------------

  403 despues de cambiar el token
      -> no se reinicio daphne, o hay un espacio o comilla de mas en el .env

  404 en /api/v1/sync/...
      -> el git pull no entro; revisar config/urls.py linea 34

  "La sincronizacion esta deshabilitada"
      -> el SYNC_TOKEN del servidor quedo vacio

  daphne no levanta
      -> mirar logs\daphne.log. Necesita los DOS endpoints, tcp y tcp6: los
         nombres de red resuelven primero a IPv6

  las tablets no entran
      -> Vite necesita host '::' y el hostname en server.allowedHosts
         (Vite 5.4.12+ rechaza hostnames con 403)

  volver todo atras
      -> respaldo\restaurar.bat con la carpeta del paso 1


================================================================================
 RESPUESTA DEL SERVIDOR - completar aca y commitear
================================================================================

  Fecha y hora: 12/09/2026, ~15:32 (hora del servidor)

  [x] Paso 1 - respaldo hecho, carpeta:
      respaldos\respaldo_20260912_153007  (54,2 MB: base + 528 fotos)

  [x] Paso 2 - git pull. Commit en el que quedo: f64e053 (este mismo commit
      con las instrucciones, ya bajado antes de tocar nada)
      Habia cambios locales sin commitear? No. El working tree estaba limpio
      (aparte de fotos de catalogo sin trackear en backend/media/productos/,
      que no tienen nada que ver con esto).
      Ojo: la ruta de este equipo NO es C:\ceramica_final como asume el
      instructivo, es C:\Users\Usuario\ogapora_sistema\Proyectos-de-Trabajo -
      los comandos de este archivo se corrieron adaptando la ruta.

  [x] Paso 3 - el SYNC_TOKEN del servidor: NO coincidia. Antes de leer este
      archivo ya se le habia generado un token nuevo al servidor (por otra
      sesion de Claude, sin saber que la notebook ya tenia uno en uso) -
      empezaba con "2uXD", 43 caracteres. Se reemplazo por el de la
      notebook (recibido por fuera de este archivo, a mano, como pide el
      punto 3) y se verifico que tambien tiene 43 caracteres.

  [x] Paso 4 - migrate y migrate --database=sync, salida:
      Las dos: "No migrations to apply." (ya estaban al dia de antes en
      esta sesion, aplicadas junto con la migracion de sync.sqlite3).

  [x] Paso 4 - detener + iniciar (via detener_servicios.ps1 /
      iniciar_servicios.ps1, agregados en 8420d61): ok, daphne y Vite
      arriba y ocultos.

  [x] Paso 5 - /api/v1/sync/estado/ contra localhost devolvio: 200

  Otras cosas que aparecieron:
  - CONTACTO_COMERCIAL (direccion/telefono del pie de ticket y nota de
    pedido) tambien se corrigio en 4d32faf, por si se nota distinto en un
    presupuesto impreso desde la notebook.
  - Listo para que la notebook siga con sync_comparar / sync_empujar
    (paso 5 de este instructivo, del lado de alla).


================================================================================
