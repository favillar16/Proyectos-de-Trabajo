================================================================================
  REPARAR LA ACTUALIZACION - INSTRUCCIONES PARA LA PC SERVIDOR (OGAPORA)
================================================================================

  Actualizado: 12/09/2026 ~19:00, desde la notebook (DESKTOP-UAIGET9)
  Para:        la consola de Claude que corra EN LA PC SERVIDOR (OGAPORA)

  >> EL TOKEN YA ESTA ARREGLADO Y EL EMPUJE YA SE HIZO. <<
  Los pasos 0 a 5 de la version anterior estan CUMPLIDOS. Lo que queda es la
  PARTE B, mas abajo: dos cargas desde CSV y el stock de 29 variantes.


--------------------------------------------------------------------------------
 A. LO QUE YA PASO - contexto, no hay que ejecutar nada de esto
--------------------------------------------------------------------------------

  El SYNC_TOKEN quedo igual en los dos equipos y el canal funciona
  (/api/v1/sync/estado/ devuelve 200).

  Al comparar los catalogos aparecio que NO COMPARTIAN NI UN uid: cada maquina
  genero los suyos al correr la migracion del sync sobre un catalogo que ya
  existia. Para el sync eran dos catalogos ajenos. Empujar los 918 cambios
  pendientes habria creado 564 productos duplicados en el servidor.

  Lo medido el 12/09/2026:

    Servidor antes : 601 productos / 712 variantes
    Notebook       : 564 productos / 666 variantes
    uid en comun   : 0 de 1730 filas
    De la notebook : 487 ya existian alla, 77 eran genuinamente nuevos

  DECISION DE LA PROPIETARIA: **manda el servidor**. El catalogo del servidor
  es el bueno; se descartan las 131 diferencias de precio que tenia la
  notebook. Solo viajan los productos que el servidor no tenia.

  Lo que se hizo en la notebook:

    - Se neutralizaron 839 cambios (marcados como empujados sin mandarlos), los
      de productos que ya existian alla o que los recrea un CSV. Nunca van a
      viajar, ni en la corrida automatica.
    - Se empujaron 101 cambios: 31 productos que existian SOLO en la notebook
      (cargados a mano antes de que el registro de cambios existiera), sus 31
      variantes, sus 22 fotos, y las 9 categorias / 7 marcas / 1 acabado que
      referencian. Resultado: 101 aplicados, 0 en conflicto.

    Servidor despues: 632 productos / 743 variantes / 540 imagenes.

  Los SKU de 12 de esas variantes se REGENERARON al entrar (chocaban con SKU
  que el servidor ya usaba). Es el comportamiento de diseno de
  apps/sync/conciliacion.py, no un error. Importa para el paso B.3: el stock se
  empareja por uid, NO por SKU.

  Respaldos previos de la notebook en C:\Users\usuario\respaldos\sync_20260912\
  (ceramica_db_antes.dump + sync_antes.sqlite3).


================================================================================
 B. LO QUE FALTA HACER EN EL SERVIDOR
================================================================================

 B.0 - Respaldo y codigo
--------------------------------------------------------------------------------

      cd C:\Users\Usuario\ogapora_sistema\Proyectos-de-Trabajo
      respaldo\respaldo.bat D:\        :: o sin argumento, va a ..\respaldos\
      git pull

  El pull trae el comando nuevo cargar_stock_por_uid y el CSV que usa.
  Anotar la carpeta del respaldo en la seccion de respuesta del final.


 B.1 - Los 42 productos del lote de agosto, CON sus cantidades
--------------------------------------------------------------------------------

  Estos 42 NO se empujaron a proposito, justamente para que los cree este
  comando y entren con su stock. Si se hubieran empujado primero, el comando
  los veria como "ya existe" y se saltearia las cantidades (es lo que venia
  trabando esta carga desde agosto).

      cd C:\Users\Usuario\ogapora_sistema\Proyectos-de-Trabajo\backend
      venv\Scripts\activate
      python manage.py cargar_lote_facturas --margen 40 --dry-run
      python manage.py cargar_lote_facturas --margen 40

  IMPORTANTE: **sin** --sin-stock. El margen 40 lo eligio la propietaria y es
  el mismo que se uso en la corrida de la notebook: no cambiarlo.

  Esperado: ~42 productos nuevos y el resto informado como "= ya existe".
  Si dice que crea muchos mas de 42, PARAR y avisar.


 B.2 - Los 4 productos del CSV de referencias
--------------------------------------------------------------------------------

      python manage.py cargar_referencias_productos
      python manage.py vincular_imagenes_referencia

  El segundo engancha las fotos de la carpeta "Referencias de Productos" (raiz
  del repo) con los productos. Es idempotente: lo ya vinculado lo informa y no
  lo vuelve a subir.


 B.3 - El stock de las 29 variantes que vinieron de la notebook
--------------------------------------------------------------------------------

  Los 31 productos que se empujaron entraron con stock 0, porque el stock no
  viaja por el sync (es del servidor por diseno). 29 de ellos tienen cantidades
  que la propietaria habia cargado a mano en la notebook, y hay que traerlas.

      python manage.py cargar_stock_por_uid --dry-run
      python manage.py cargar_stock_por_uid

  Lee backend\data_carga\stock_31_notebook.csv, que viene en el repo, y
  empareja por uid (no por SKU: ver la nota del final de la parte A).
  Es idempotente: si una variante ya tiene stock distinto de cero, la saltea.

  Esperado: "29 cargadas, 0 salteadas, 0 con error".
  Si dice "no existe aca la variante <uid>", el empuje no llego completo:
  PARAR y avisar.


 B.4 - Verificacion final
--------------------------------------------------------------------------------

      python manage.py shell -c "from apps.productos.models import Producto, Variante; from apps.inventario.models import Stock; print('productos', Producto.objects.count()); print('variantes', Variante.objects.count()); print('variantes sin fila de stock', Variante.objects.filter(stock__isnull=True).count())"

  Esperado, aproximado: ~678 productos, ~800 variantes, 0 variantes sin fila
  de stock. Los numeros exactos dependen de cuantas filas del lote agrupo el
  comando; lo que importa es que no haya variantes sin stock y que no se haya
  duplicado nada.

  Para descartar duplicados, contar nombres repetidos:

      python manage.py shell -c "from apps.productos.models import Producto; from collections import Counter; c=Counter(p.nombre.strip().lower() for p in Producto.objects.all()); d={k:v for k,v in c.items() if v>1}; print(len(d),'nombres repetidos'); [print(' ',v,k[:60]) for k,v in list(d.items())[:15]]"

  Antes de empezar habia 4 nombres repetidos conocidos y legitimos (bacha
  ceramica sobreponer x2, conjunto icasa 4x3 piezas, mingitorio icasa con
  sifon). Si aparecen muchos mas, algo se duplico: avisar antes de seguir.


 B.5 - Reiniciar
--------------------------------------------------------------------------------

      cd C:\Users\Usuario\ogapora_sistema\Proyectos-de-Trabajo
      detener.bat
      iniciar.bat


--------------------------------------------------------------------------------
 C. LO QUE NO HAY QUE HACER
--------------------------------------------------------------------------------

  - NO correr `sync_comparar --marcar` en la notebook. Con los catalogos como
    estaban marcaba las 1730 filas para empujar y reproducia la duplicacion.
    Despues del pg_dump de la notebook deja de ser peligroso, pero por ahora no.

  - NO volver a correr cargar_lote_facturas DESPUES de que la notebook haya
    hecho su pg_dump, esperando que cargue stock: para entonces los productos
    ya existen y el comando saltea las cantidades.

  - Los 134 "conflictos sin ver" que muestra /api/v1/sync/estado/ son del
    primer intento de empuje, que fallo con NO_EXISTE porque las categorias
    todavia no habian viajado. Son basura historica, ya resuelta: el segundo
    intento aplico los 101 cambios sin conflicto. Se pueden ignorar.


================================================================================
 RESPUESTA DEL SERVIDOR - completar aca y commitear
================================================================================

  Fecha y hora:

  [ ] B.0 - respaldo, carpeta:
      git pull, commit en el que quedo:

  [ ] B.1 - cargar_lote_facturas --margen 40, resumen de la corrida:

  [ ] B.2 - cargar_referencias_productos / vincular_imagenes_referencia:

  [ ] B.3 - cargar_stock_por_uid, resumen (se esperan 29 cargadas):

  [ ] B.4 - productos: ____  variantes: ____  sin stock: ____
      nombres repetidos: ____ (habia 4 antes)

  [ ] B.5 - detener.bat + iniciar.bat, ok?

  Otras cosas que aparecieron:



================================================================================

================================================================================
 LO QUE EL SERVIDOR YA REPORTO (commit 31f0d86, 12/09/2026 15:32)
================================================================================

  Respondido sobre la version anterior de este archivo, antes del empuje.
  Se conserva porque tiene datos que siguen valiendo:

  - Respaldo previo: respaldos\respaldo_20260912_153007 (54,2 MB, base + 528
    fotos).
  - La ruta del proyecto en el servidor NO es C:\ceramica_final: es
    C:\Users\Usuario\ogapora_sistema\Proyectos-de-Trabajo. Este archivo ya
    quedo corregido con esa ruta.
  - El token no coincidia porque OTRA sesion de Claude le habia generado al
    servidor un token nuevo de cero (empezaba con "2uXD"), sin saber que la
    notebook ya tenia uno en uso. Se reemplazo por el de la notebook.
  - migrate y migrate --database=sync: "No migrations to apply", ya estaban
    al dia.
  - Working tree limpio salvo fotos de catalogo sin trackear en
    backend/media/productos/, que no tienen que ver con esto.
  - /api/v1/sync/estado/ contra localhost: 200.

